{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "DISPLAY=:99 CHROME_BIN=$(which chromium) CHROMEDRIVER_PATH=$(which chromedriver) HOME=/tmp TMPDIR=/tmp uvicorn app:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
